[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    slow: marks tests that exercise the full Cartesian product, chart
          generation, or other multi-second workloads; skip with
          -m "not slow" for a fast feedback loop
